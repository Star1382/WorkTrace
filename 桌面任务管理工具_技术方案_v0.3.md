# WorkTrace — 完整技术规格 v0.3

> 生成时间：2026-05-22
> 更新时间：2026-05-23（v0.3：补全IPC接口规格、周报/月报模块、提醒引擎、交互规范；调整开发优先级）
> 状态：开发中（模块1 MVP、Phase 1、Phase 2、Phase 3、Phase 4 已完成；Phase 5 已完成快捷键和右键菜单边界；模块5知识库接口暂未开发）
> 仓库：https://github.com/Star1382/WorkTrace

---

## 一、产品定位

**央企行政人员的可视化工作台**——不是命令行工具，是桌面程序。

核心原则：**看见就能用，不是记住才能用。**
- 打开第一眼 → 日历视图，今日待办一目了然
- 点一下 → 添加/完成任务
- 拖一下 → 改优先级、改日期、改状态
- 周五一键 → 出周报
- 月末一键 → 出月报

具体解决什么：
- 做之前记 → 提醒你什么时候开始
- 做之后记 → 自动归档留痕
- 忘了做了什么 → 日历+列表双视图回溯
- 周报发愁 → 一键生成
- 月报发愁 → 一键生成

---

## 二、技术选型

| 维度 | 选择 | 理由 |
|------|------|------|
| 框架 | **Electron** | 你不会Rust，Codex能直接写JS，生态最成熟 |
| 前端 | React + Tailwind | 轻量、Codex擅长 |
| 数据库 | SQLite（better-sqlite3） | 本地存储、零配置、够快 |
| 打包 | electron-builder | 成熟方案 |

---

## 三、项目结构（当前）

```
worktrace/
├── electron/
│   ├── main.cjs              # 主进程：初始化DB → 注册模块 → 创建窗口
│   ├── preload.cjs           # 预加载：暴露IPC给渲染进程
│   ├── database.cjs          # DB初始化 + 连接管理
│   └── modules/
│       ├── index.cjs         # 模块注册入口
│       ├── task.module.cjs   # 任务CRUD
│       └── stats.module.cjs  # 统计查询
├── src/
│   ├── App.jsx               # 主界面：三栏布局 + 状态管理
│   ├── index.css             # Tailwind入口
│   ├── main.jsx              # React入口
│   ├── components/
│   │   ├── Calendar.jsx      # 月历组件
│   │   ├── TaskList.jsx      # 任务列表 + 右键菜单
│   │   ├── TaskModal.jsx     # 添加/编辑任务弹窗
│   │   └── StatusBar.jsx     # 底部状态栏
│   └── services/
│       ├── ipc.js            # IPC调用封装
│       ├── taskService.js    # 任务服务
│       └── statsService.js   # 统计服务
├── index.html
├── package.json
├── vite.config.js
├── tailwind.config.js
└── postcss.config.js
```

---

## 四、数据库设计（SQLite）

### 现有表

```sql
CREATE TABLE tasks (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  title TEXT NOT NULL,
  description TEXT,
  quadrant INTEGER DEFAULT 0,    -- 0=未分类 1=紧急重要 2=紧急不重要 3=重要不紧急 4=不重要不紧急
  status TEXT DEFAULT 'todo',    -- todo/in_progress/done/stuck/cancelled
  due_date DATE,
  remind_at DATETIME,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  completed_at DATETIME
);
```

### 远期扩展（当前不做）

```sql
CREATE TABLE tags (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  name TEXT UNIQUE NOT NULL
);

CREATE TABLE task_tags (
  task_id INTEGER REFERENCES tasks(id),
  tag_id INTEGER REFERENCES tags(id),
  PRIMARY KEY (task_id, tag_id)
);
```

---

## 五、IPC接口规格

### 5.1 任务模块（task.module.cjs）

| 通道 | 参数 | 返回 | 说明 |
|------|------|------|------|
| `task:getByDate` | `{ date: "2026-05-23" }` | `{ success, data: Task[] }` | 按日期查任务，按created_at升序 |
| `task:add` | `{ title, description?, quadrant?, status?, due_date }` | `{ success, data: { id } }` | 新增任务 |
| `task:update` | `{ id, title, description?, quadrant, status, due_date, remind_at? }` | `{ success }` | 更新任务（⚠️当前缺due_date和remind_at，需修复） |
| `task:toggleStatus` | `(taskId, newStatus)` | `{ success }` | 切换状态，done时写completed_at，非done时清空 |
| `task:delete` | `(taskId)` | `{ success }` | 删除任务 |

### 5.2 统计模块（stats.module.cjs）

| 通道 | 参数 | 返回 | 说明 |
|------|------|------|------|
| `stats:getQuadrant` | 无 | `{ success, data: { 1:n, 2:n, 3:n, 4:n } }` | 未完成任务按象限统计 |
| `stats:getWeek` | 无 | `{ success, data: { total, done } }` | 本周任务统计 |

### 5.3 周报模块（新增 report.module.cjs）

| 通道 | 参数 | 返回 | 说明 |
|------|------|------|------|
| `report:weekly` | `{ date?: "2026-05-23" }` | `{ success, data: WeeklyReport }` | 默认本周，可指定日期所在周 |
| `report:monthly` | `{ date?: "2026-05-23" }` | `{ success, data: MonthlyReport }` | 默认本月，可指定日期所在月 |
| `report:exportText` | `{ type: "weekly"|"monthly", date? }` | `{ success, data: { text, filename } }` | 生成格式化文本 |
| `report:copyToClipboard` | `{ text }` | `{ success }` | 写入系统剪贴板 |
| `report:saveToFile` | `{ text, filename, defaultPath? }` | `{ success, data: { path } }` | 弹出保存对话框 |

**WeeklyReport 格式：**
```json
{
  "period": "2026-05-19 ~ 2026-05-25",
  "total": 15,
  "done": 10,
  "in_progress": 2,
  "todo": 1,
  "stuck": 1,
  "cancelled": 1,
  "quadrant_breakdown": { "1": 3, "2": 2, "3": 6, "4": 2, "0": 2 },
  "tasks": [
    { "title": "提交月度报表", "status": "done", "quadrant": 1, "due_date": "2026-05-20", "completed_at": "2026-05-20 10:30:00" },
    ...
  ]
}
```

**MonthlyReport 格式：**
```json
{
  "period": "2026-05-01 ~ 2026-05-31",
  "total": 45,
  "done": 32,
  "in_progress": 5,
  "todo": 4,
  "stuck": 2,
  "cancelled": 2,
  "quadrant_breakdown": { "1": 8, "2": 5, "3": 20, "4": 8, "0": 4 },
  "weekly_trend": [
    { "week": "5.1-5.4", "total": 10, "done": 7 },
    { "week": "5.5-5.11", "total": 12, "done": 9 },
    ...
  ],
  "tasks": [...]
}
```

---

## 六、模块规格

### 模块1：任务录入 + 今日视图 ✅（MVP已完成）

已实现功能：
- 左侧月历 + 四象限统计
- 右侧任务列表（按日期）
- 添加/编辑任务弹窗
- 右键菜单切换状态
- 勾选完成/删除

**需修复：**
- `task:update` 缺少 due_date 和 remind_at 字段更新
- 右键菜单可能跑出屏幕边界（体验优化，优先级低）

### 模块2：四象限看板（模块1修完后开发）

**界面：** 右侧主区域新增「看板」视图tab

```
┌─────────────┬─────────────┐
│  紧急重要    │  紧急不重要  │
│  ┌─────┐    │  ┌─────┐    │
│  │任务1│    │  │任务3│    │
│  │任务2│    │  └─────┘    │
│  └─────┘    │             │
├─────────────┼─────────────┤
│  重要不紧急  │ 不重要不紧急 │
│  ┌─────┐    │  ┌─────┐    │
│  │任务4│    │  │任务5│    │
│  └─────┘    │  └─────┘    │
└─────────────┴─────────────┘
```

**IPC新增：**

| 通道 | 参数 | 返回 | 说明 |
|------|------|------|------|
| `task:getByQuadrant` | `{ excludeDone?: true }` | `{ success, data: Task[] }` | 按象限查任务 |

**交互：**
- 拖拽任务卡片到其他象限 → 更新 quadrant
- 使用 `@dnd-kit/core` + `@dnd-kit/sortable`
- 拖拽结束后调用 `task:update` 更新 quadrant

### 模块3：提醒引擎（依赖 remind_at 字段修复）

**实现方式：** Electron主进程定时器

**IPC新增：**

| 通道 | 参数 | 返回 | 说明 |
|------|------|------|------|
| `reminder:check` | 无 | `{ success, data: Task[] }` | 查询到期未提醒的任务 |
| `reminder:setSnooze` | `(taskId, minutes)` | `{ success }` | 贪睡N分钟后重新提醒 |

**逻辑：**
1. 主进程每1分钟查一次：`SELECT * FROM tasks WHERE remind_at <= datetime('now') AND status != 'done'`
2. 命中 → Windows系统通知（`Notification` API）+ 可选声音提醒
3. 点击通知 → 窗口前置并高亮该任务
4. 贪睡 → 更新 remind_at 为 now + N分钟

**任务弹窗新增：**
- remind_at 输入框：日期+时间选择器
- 预设按钮：「1小时后」「明天9:00」「下周一9:00」

### 模块4：周报 + 月报生成（当前优先开发）

**界面：** 右侧主区域顶部新增tab切换

```
┌─────────────────────────────────────┐
│  [今日]  [看板]  [周报]  [月报]     │
├─────────────────────────────────────┤
│                                     │
│  （对应视图内容）                    │
│                                     │
└─────────────────────────────────────┘
```

**周报视图：**
```
┌─────────────────────────────────────┐
│  📊 本周汇报  5.19-5.25             │
│  完成率：10/15 (67%) ████████░░░    │
│─────────────────────────────────────│
│  一、紧急重要（3项）                 │
│    ✓ 提交月度报表        5.20       │
│    ✓ 会议室预约          5.21       │
│    ☐ 跟进供应商合同      5.23       │
│                                     │
│  二、紧急不重要（2项）               │
│    ✓ 复印文件            5.22       │
│    ◐ 整理文件柜          5.24       │
│                                     │
│  三、重要不紧急（6项）               │
│    ✓ 整理档案            5.19       │
│    ...                              │
│                                     │
│  四、不重要不紧急（2项）             │
│    ...                              │
│                                     │
│  📋 待推进事项                       │
│    ⚠ 跟进供应商合同（阻塞）         │
│    ☐ 资产盘点（待办）               │
│─────────────────────────────────────│
│  [📋 复制到剪贴板]  [💾 导出文件]   │
└─────────────────────────────────────┘
```

**月报视图：**
```
┌─────────────────────────────────────┐
│  📊 本月汇报  2026年5月              │
│  完成率：32/45 (71%) █████████░░    │
│  环比上月：↑5%                      │
│─────────────────────────────────────│
│  周度趋势                           │
│  5.1-5.4   ████████░░  7/10        │
│  5.5-5.11  █████████░  9/12        │
│  5.12-5.18 ████████░░  8/11        │
│  5.19-5.25 ████████░░  8/12        │
│─────────────────────────────────────│
│  （按象限分组任务清单，同周报格式）  │
│─────────────────────────────────────│
│  [📋 复制到剪贴板]  [💾 导出文件]   │
└─────────────────────────────────────┘
```

**复制到剪贴板的文本格式（给领导看的）：**

```
【本周工作汇报】2026.5.19-5.25
完成率：10/15 (67%)

一、紧急重要（3项）
✓ 提交月度报表 5.20
✓ 会议室预约 5.21
☐ 跟进供应商合同 5.23

二、紧急不重要（2项）
✓ 复印文件 5.22
◐ 整理文件柜 5.24（进行中）

三、重要不紧急（6项）
✓ 整理档案 5.19
...

四、不重要不紧急（2项）
...

待推进事项：
⚠ 跟进供应商合同（阻塞）
☐ 资产盘点（待办）
```

**状态符号约定：**
- ✓ = done
- ◐ = in_progress
- ☐ = todo
- ⚠ = stuck
- ✗ = cancelled

**新增前端组件：**
- `src/components/WeeklyReport.jsx`
- `src/components/MonthlyReport.jsx`
- `src/services/reportService.js`

**新增后端模块：**
- `electron/modules/report.module.cjs`

### 模块5：知识库接口（远期，暂不开发）

```
桌面工具录入 → SQLite本地存储 → 定期导出 → Dify向量库 → AI检索
                                                    ↓
                                              周报/月报/年度总结
                                                    ↓
                                              职业知识库（"我怎么干活"）
```

---

## 七、交互规范

### 7.1 快捷键（优先级低，体验优化阶段再加）

| 快捷键 | 功能 | 上下文 |
|--------|------|--------|
| `Ctrl+N` | 新建任务 | 全局 |
| `Delete` | 删除选中任务 | 任务列表焦点时 |
| `Enter` | 编辑选中任务 / 保存弹窗 | 任务列表 / 弹窗内 |
| `Esc` | 关闭弹窗/菜单 | 弹窗/菜单打开时 |
| `Ctrl+1~4` | 切换右侧视图tab | 全局 |

### 7.2 右键菜单边界修正（优先级低）

当前问题：菜单可能跑出屏幕。

修正逻辑：
```js
const menuWidth = 180;
const menuHeight = 200;
const { innerWidth, innerHeight } = window;

let x = e.clientX;
let y = e.clientY;

if (x + menuWidth > innerWidth) x = innerWidth - menuWidth - 8;
if (y + menuHeight > innerHeight) y = innerHeight - menuHeight - 8;
```

### 7.3 拖拽规范（模块2时实现）

- 拖拽距离 > 5px 才触发（防误触）
- 拖拽中卡片半透明 + 阴影
- 放下时更新 quadrant 并调用 `task:update`
- 放到无效区域 → 弹回原位

---

## 八、开发优先级（v0.3调整）

```
Phase 1 — 修基础（30分钟）
  ├── 修 task:update：补 due_date + remind_at 字段
  └── preload.cjs：暴露 report 相关 IPC

Phase 2 — 周报+月报（核心价值，2-3小时）
  ├── 新增 report.module.cjs
  ├── 新增 reportService.js
  ├── 新增 WeeklyReport.jsx + MonthlyReport.jsx
  ├── App.jsx：加 tab 切换
  ├── 复制到剪贴板
  └── 导出文件（Electron dialog）

Phase 3 — 四象限看板（1-2小时）
  ├── task:getByQuadrant IPC
  ├── 新增 QuadrantBoard.jsx
  ├── 拖拽（@dnd-kit）
  └── tab 切换集成

Phase 4 — 提醒引擎（1-2小时）
  ├── 主进程定时器
  ├── Windows 通知
  ├── remind_at 输入控件
  └── 贪睡功能

Phase 5 — 体验打磨（随时间渐进）
  ├── 快捷键
  ├── 右键菜单边界
  ├── 动画过渡
  └── 窗口常驻/最小化到托盘
```

### 8.1 当前实现状态（2026-05-23）

已完成：
- Phase 1：`task:update` 已补 `due_date` / `remind_at`，任务弹窗已支持截止日期和提醒时间。
- Phase 2：周报/月报 IPC、前端服务、视图、复制剪贴板和导出文件已完成。
- Phase 3：`task:getByQuadrant`、`QuadrantBoard.jsx`、`@dnd-kit` 拖拽和“看板”tab 已完成。
- Phase 4：`reminder:check`、`reminder:setSnooze`、主进程定时器、系统通知、通知点击前置窗口和任务高亮已完成。
- Phase 5：快捷键和右键菜单边界已完成。

未完成：
- 模块5知识库接口：仍按远期处理。
- Phase 5 远期体验项：动画过渡、窗口常驻、最小化到托盘尚未开发。
- 打包发布和自动化测试尚未开发。

---

## 九、已知问题

| 问题 | 影响 | 优先级 |
|------|------|--------|
| task:update 缺 due_date/remind_at | 已修复 | ✅ 已完成 |
| 右键菜单可能跑出屏幕 | 已修复 | ✅ 已完成 |
| 仓库GitHub 404 | 主分支是master不是main，ZIP下载链接需用master | 🟢 已知 |
| 模块5知识库接口未开发 | 远期能力，不影响桌面任务工作台主流程 | 🟡 待规划 |
| 未打包发布 | 只能开发模式运行，未生成安装包 | 🟡 待做 |
| 未新增自动化测试 | 后续改动缺少回归保护 | 🟡 待做 |

---

*荣耀确认后，Codex按Phase 1→2顺序开发。*
